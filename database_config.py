TABLES = {
    "Members": 'CREATE TABLE "Members" ("UserID" INTEGER NOT NULL UNIQUE, "Username" TEXT NOT NULL, "NumLogs" INTEGER NOT NULL, "MsgsSent"	INTEGER NOT NULL, PRIMARY KEY("UserID"));',
    "ModLogs": 'CREATE TABLE "ModLogs" ("ID" INTEGER NOT NULL UNIQUE, "UserID" INTEGER NOT NULL, "ModeratorID" INTEGER NOT NULL, "Reason" TEXT NOT NULL, "Date" TEXT NOT NULL, PRIMARY KEY("ID"))',
    "Reports": 'CREATE TABLE "Reports" ("ID" INTEGER NOT NULL UNIQUE, "ReporterID" INTEGER NOT NULL, "UserID" INTEGER NOT NULL, "Reason" TEXT NOT NULL, "Date" TEXT, "Resolved" INTEGER NOT NULL DEFAULT 0, PRIMARY KEY("ID"))',
    "Roles": 'CREATE TABLE "Roles" ("RoleID" INTEGER NOT NULL UNIQUE, "RoleName" TEXT NOT NULL UNIQUE, "RoleKey" TEXT NOT NULL UNIQUE, PRIMARY KEY("RoleID"))'
}
